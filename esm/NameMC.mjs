import NameMCWrapper from "../dist/NameMC.js";

export default NameMCWrapper;

export const {
    NameMC,
    API,

    CapeContext,
    PlayerContext,
    SkinContext,
    ServerContext,
    RendersContext,

    capeHash,
    capeType,
    capeName,
    model
} = NameMCWrapper;
